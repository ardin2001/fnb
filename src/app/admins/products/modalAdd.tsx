export default function ModalAdd({addRef}: any) {
    return (
        <div ref={addRef} className="bg-white shadow-furdamental fixed top-0 left-0 z-50" style={{display: "none"}}>
            <h1>Modal Add</h1>
        </div>
    )
}